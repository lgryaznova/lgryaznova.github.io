В хроме по умолчанию, политиками безопасноти запрещено загружать данные локально,
не из http(s), ftp и т.д.

Поэтому я распарсил json (с помощью parseJSON.py в into_html.data), стянутый из источника, что выдала, в принципе легче было-бы работать напрямую с geojson, но необходимо для него регистрироваться, а также есть вероятность, что он также как и json, в виде архива.

Для отображения необходимы координаты, а также их название.


